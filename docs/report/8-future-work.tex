\section{Hạn chế \& Hướng phát triển}

- Chỉ parse được một ngôn ngữ lập trình do java chỉ có một thư viện java parser -> implement một service khác bằng python có thể parse được ngôn ngữ khác

- linking cross artifact chỉ dựa trên ref, đúng tên, keyword chứ không dùng semantic. Lý do là có thể dùng semantic search thì cũng sẽ ra kết quả từ nhiều nguồn, và trade off là thà không có còn hơn là không đúng -> phát triển LLM-based


