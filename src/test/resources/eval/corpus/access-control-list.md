# Access Control Lists

An access control list records which principals may read which sources. Queries are scoped to the caller's readable set so a disallowed source never appears in results, whether reached by semantic, keyword, or graph paths.
