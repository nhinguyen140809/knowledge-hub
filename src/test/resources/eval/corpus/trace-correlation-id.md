# Trace Correlation Id

A trace correlation id stamped on each request threads through every log line for that request. With one id an operator can reconstruct a single failed request end to end, even across the authentication boundary.
