# Tokenization

Tokenization breaks text into the units a model counts and processes. A chunk budget is measured in tokens rather than characters, because model context limits and embedding inputs are token based.
