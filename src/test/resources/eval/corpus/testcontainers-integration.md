# Testcontainers Integration Testing

Testcontainers runs a real database in a throwaway container for integration tests, exercising actual wiring instead of mocks. Pinning the image version keeps the tests reproducible across machines and CI.
