# Neo4j Graph Database

Neo4j stores data as nodes and relationships with properties, and traverses connections natively without join tables. It suits knowledge graphs where relationships between entities are first-class and multi-hop traversal is common.
