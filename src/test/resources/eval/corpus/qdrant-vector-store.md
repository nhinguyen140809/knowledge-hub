# Qdrant Vector Store

Qdrant is a vector database that stores embeddings with payload metadata and serves similarity search over gRPC or REST. Collections define the vector dimension and distance metric, and payload filters restrict results before scoring.
