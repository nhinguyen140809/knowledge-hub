# API mode:
#   real -> the app talks to a live backend (enter its URL + admin key to connect)
#   mock -> the app serves built-in mock data; any URL/key is accepted, no backend
# Override locally without touching this default by setting it in .env.local
# (gitignored). Restart the dev server after changing env values.
VITE_API_MODE=mock
