# RFC 7807 Problem Detail

RFC 7807 defines a standard problem detail body with type, title, status, and detail fields for API errors. A shared error shape with a stable code lets a client branch on the same value at every boundary.
