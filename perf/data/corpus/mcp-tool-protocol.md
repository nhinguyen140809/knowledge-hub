# Model Context Protocol Tools

The Model Context Protocol lets an agent call server-side tools with typed parameters over a streamable transport. Each tool is an inbound adapter over an existing application service, so the same logic serves REST and MCP callers.
