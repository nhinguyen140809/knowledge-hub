# Semantic Versioning

Semantic versioning numbers a release major dot minor dot patch. A major bump signals a breaking change, a minor adds backward-compatible features, and a patch fixes bugs without changing the interface.
