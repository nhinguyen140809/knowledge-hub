# Dependency Injection

Dependency injection supplies a component's collaborators from outside rather than having it construct them. Depending on ports rather than concrete adapters lets an implementation be swapped by configuration alone.
