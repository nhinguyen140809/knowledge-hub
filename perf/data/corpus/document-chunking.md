# Document Chunking

Chunking splits a document into passages small enough to embed and retrieve precisely. Overlapping windows preserve context across boundaries, while code is chunked along syntactic units so a function stays intact.
