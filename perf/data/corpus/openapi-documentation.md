# OpenAPI Documentation

An OpenAPI description documents every endpoint, its schema, and examples so clients can be generated automatically. Grouping query and admin endpoints separately keeps the reference navigable for different audiences.
