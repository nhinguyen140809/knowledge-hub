# HNSW Approximate Nearest Neighbour

Hierarchical Navigable Small World builds a layered proximity graph for approximate nearest neighbour search. Queries descend from a sparse top layer to dense lower layers, trading a small amount of recall for a large speed-up over exhaustive scans.
