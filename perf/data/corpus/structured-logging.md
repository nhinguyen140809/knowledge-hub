# Structured Logging

Structured logging emits parameterised events with stable fields instead of ad-hoc strings. Consistent fields let an operator filter and aggregate logs, and lifecycle events are logged at info while developer detail sits at debug.
