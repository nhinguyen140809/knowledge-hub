# Vector Embeddings

A vector embedding maps text into a dense numeric vector so that semantically similar passages land near each other. Embeddings power semantic search, where a query vector is compared against stored passage vectors instead of matching literal words.
